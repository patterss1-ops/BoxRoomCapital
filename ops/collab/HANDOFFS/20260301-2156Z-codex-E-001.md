# E-001 Handoff — Signal Contracts + Scoring Schema

## Context
Phase E split is ACKed. `E-001` freezes shared Signal Engine contract types so layer tickets (`E-002/E-003/E-004/E-005`) can proceed in parallel without schema drift.

## Changes
1. Added new signal package:
   - `app/signal/types.py`
   - `app/signal/contracts.py`
   - `app/signal/__init__.py`

2. Added contract primitives:
   - Canonical layer IDs (`l1_pead` .. `l8_sa_quant`)
   - Canonical decision actions (`auto_execute_buy`, `flag_for_review`, `short_candidate`, `no_action`)
   - Decision threshold contract (`ScoreThresholds`)
   - Default layer weights matching current Signal Engine specification

3. Added validated payload contracts:
   - `LayerScore` with strict range/format validation and dict round-trip
   - `CompositeRequest` with duplicate-layer guard and ticker/as_of consistency checks
   - `CompositeResult` output contract and serialization
   - Helper functions: `resolve_layer_weights`, `decide_action`, `build_composite_result`

4. Added regression tests:
   - `tests/test_signal_contracts.py` (10 tests)

## Tests/Checks
- `python3 -m pytest -q tests/test_signal_contracts.py` -> 10 passed
- `python3 -m pytest -q tests` -> 548 passed

## Risks
- Weight override behavior currently renormalizes all layers if overrides are provided; this is deterministic but should be treated as contract behavior for E-006.

## Next Action
- Claude can begin E-002/E-004/E-005 against these contracts after E-001 merge.
- Codex proceeds with E-003 in parallel once E-001 is merged.

## Blockers
- None.
