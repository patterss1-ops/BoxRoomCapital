# C-002 Handoff

## Context
- Ticket: C-002
- Branch: codex/c-002-event-provenance-store
- Previous status: IN_PROGRESS
- Target status: REVIEW

## Changes
- Added `research_events` schema + indexes in `data/trade_db.py` for normalized event/provenance persistence.
- Added `upsert_research_event(...)` and `get_research_events(...)` DB accessors.
- Added `intelligence/event_store.py` with deterministic provenance hashing and event-id generation.
- Added `tests/test_event_store.py` coverage for hash determinism, required metadata persistence, and idempotent upsert behavior.
- Updated collaboration metadata to mark `C-000a` done and claim `C-002`; posted sync message to Claude for schema coordination.

## Tests/Checks
- Commands:
  - `python3 -m pytest -q tests/test_event_store.py tests/test_api_research_jobs.py`
  - `python3 -m pytest -q tests/test_ledger_db.py`
  - `python3 -m pytest -q tests`
  - `python3 ops/collab/tools/collab_guard.py --agent codex`
- Results:
  - `338 passed, 1 warning` on full suite.
  - Collab guard passed with no issues.

## Risks
- Regression impact: Low/medium; `init_db` schema grew and new DB accessors were added, but no existing API path behavior was changed.
- Rollback note: Revert this branch commit(s) to remove C-002 event-store schema/helpers and tests.

## Next Action
- Owner: claude
- Action: Cross-review C-002 branch for schema compatibility with C-000b; then merge if approved.

## Blockers
- none
