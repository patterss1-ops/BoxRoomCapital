---
ticket: G-005
title: Phase G acceptance harness + release checks
owner: claude
status: REVIEW
branch: claude/g-005-phase-g-acceptance
commit_head: tbd
date: 2026-03-03T12:00:00Z
---

## Summary

Phase G end-to-end acceptance harness covering the full signal + AI confidence
gate + execution-quality pipeline. Extends the existing release checks script
with Phase G sections.

## Deliverables

### tests/test_phase_g_e2e.py (37 tests, 9 sections)

1. **Contract immutability + round-trip** — MappingProxyType enforcement, frozen fields, verdict/consensus serialization
2. **Panel adapter contracts** — build_verdict_from_parsed, opinion coercion, confidence clamping, all 4 clients importable
3. **Panel coordinator aggregation** — unanimous buy, mixed opinions, failed model counting
4. **AI confidence calibration** — bounded output across all input ranges, confidence monotonicity, quality penalty ordering, zero-sample bypass
5. **Gate decision policy** — all reject paths (low confidence, neutral, insufficient models, missing quality, disabled gate), policy wrapper equivalence
6. **Orchestrator AI gate integration** — entry gated by low confidence, allowed by high confidence, exit bypass, missing-ticker passthrough
7. **Pipeline dispatch AI wiring** — explicit consensus forwarding, ai_panel_enabled collection trigger
8. **Execution quality feedback loop** — degraded quality can flip decision, slippage-only degradation
9. **Full Phase G regression** — multi-ticker gating (one pass/one reject), gate disabled allows all, no consensus map runs ungated, rejection metadata audit fields

### ops/collab/release-checks/signal_engine_checks.sh (sections 12-17 added)

- Section 12: Execution quality rollups (G-002)
- Section 13: AI panel contracts + adapters (G-003)
- Section 14: AI confidence gate (G-004)
- Section 15: Phase G E2E acceptance harness (G-005)
- Section 16: Phase G module import smoke tests (11 modules)
- Section 17: Phase G source file presence (13 files)

## Validation

```
python -m pytest -q tests/test_phase_g_e2e.py
37 passed in 3.86s

python -m pytest -q tests/
1128 passed in 109.63s
```

Zero regressions across the full suite.

## Rollback

Remove `tests/test_phase_g_e2e.py` and revert release-checks additions.
No production code modified.
