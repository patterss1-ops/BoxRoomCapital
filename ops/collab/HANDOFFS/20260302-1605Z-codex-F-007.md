# F-007 Handoff — Tier-1 shadow jobs + operator ranking surface

## Context
F-006 is merged on `main` (`c3059ca`). F-007 delivers tier-1 shadow orchestration and operator-facing ranked candidate/freshness diagnostics to unblock Claude F-008 acceptance work.

## Changes
- Added `intelligence/jobs/signal_layer_jobs.py`:
  - `run_tier1_shadow_jobs(...)` orchestration runner (SA Quant refresh + strict shadow cycle + derived ranking/diagnostics)
  - `build_ranked_candidates(...)` for deterministic action-aware candidate ranking
  - `summarize_freshness_diagnostics(...)` aggregate warning/stale/missing counters
  - `enrich_signal_shadow_payload(...)` to augment API/UI payloads with ranking + freshness summaries
- Updated `app/api/server.py` (signal endpoints only):
  - `/api/signal-shadow` now returns enriched payload (rankings + freshness diagnostics)
  - Added `/api/actions/signal-tier1-run` async action route
  - Added `_run_signal_tier1_job(...)` background job worker
  - Updated signal fragment context to use enriched payload
- Updated `app/web/templates/_signal_engine.html`:
  - Added Tier-1 run button
  - Added blocked-missing and blocked-stale badges
  - Added ranked candidates table
  - Added freshness diagnostics summary line
  - Status rendering now treats all `scored*` statuses as scored rows
- Expanded `tests/test_signal_shadow_api.py`:
  - route lifecycle test for `/api/actions/signal-tier1-run`
  - API enrichment test for ranked candidates/freshness diagnostics
  - fragment rendering checks for ranking + diagnostics
  - deterministic ranking helper test

## Tests/Checks
- `python3 -m pytest -q tests/test_signal_shadow_api.py` -> 9 passed
- `python3 -m pytest -q` -> 895 passed
- `python3 ops/collab/tools/collab_guard.py --agent codex` -> pass

## Risks
- Tier-1 orchestration currently has explicit ingest runner only for L8 SA Quant; other layers are surfaced as `skipped` until their ingestion runners are added.
- Strict required-layer behavior is enforced by `run_tier1_shadow_jobs` (`enforce_required_layers=True`) and may reduce ranked output until all layer feeds are available per ticker.

## Next Action
Claude cross-review on PR for F-007; after merge, Claude can proceed with F-008 acceptance harness/release checks.

## Blockers
None.
