## Context
Ticket `A-010` closes the remaining Phase A control-plane gap by exposing unified ledger/NAV data directly in the operator UI.

## Changes
- Added ledger fragment endpoint in `app/api/server.py`:
  - `GET /fragments/ledger`
  - renders unified ledger snapshot + reconciliation summary for dashboard use
- Added new UI fragment template:
  - `app/web/templates/_ledger_snapshot.html`
  - sections:
    - unified ledger summary metrics
    - broker position table
    - recent NAV snapshot table
    - reconcile status badge + top suggestion
- Wired ledger panel into both pages:
  - `app/web/templates/overview.html`
  - `app/web/templates/trading.html`
  - adds `#ledger-panel` with HTMX polling from `/fragments/ledger`
- Extended API/UI tests in `tests/test_api_phase_a_surfaces.py`:
  - fragment render test for ledger snapshot + reconcile state
  - page-level checks ensuring ledger panel is present on Overview and Trading

## Tests/Checks
- `python3 -m pytest -q tests/test_api_phase_a_surfaces.py tests/test_api_ledger.py`
  - `8 passed, 1 warning` (urllib3/OpenSSL environment warning only)
- `python3 -m pytest -q tests`
  - `51 passed, 1 warning`
- `./ops/collab/release-checks/run_phase_a_checks.sh`
  - all checks passed

## Risks
- Regression impact: low. Additive fragment + template wiring only.
- Rollback note: revert commit `b4b68e2`.
- Operational note: fragment currently renders first-level snapshot/reconcile summary; if broker data volume grows, pagination or server-side truncation may be needed.

## Next Action
- Owner: codex/claude shared
- Action: merge A-003/A-005/A-007/A-010 together and perform manual dashboard smoke check to confirm ledger panel readability with live runtime data.

## Blockers
- none
