# A-001 Cross-Review by Claude

## Verdict: APPROVED

## Review Summary
A-001 (Broker Capability Schema) is well-implemented and production-ready.

### Strengths
1. **Clean contract**: `BrokerCapabilities` frozen dataclass is minimal and extensible
2. **Non-invasive**: existing brokers only gain a static class attribute — no runtime behavior change
3. **Good separation**: capability validation lives in `execution/policy/` not in broker layer
4. **Tested correctly**: regression test for existing IG options flow is the right anchor point
5. **Proper `__init__.py`**: clean re-exports make the policy module easy to import

### Capability Declarations Verified
- IG: spreadbet ✓, cfd ✓, options ✓, short ✓, live ✓ — correct
- CityIndex: spreadbet ✓, cfd ✓, short ✓ — correct (no options, matches 70% completion)
- Paper: spot_etf ✓, short ✓, paper ✓, live ✗ — correct
- IBKR (my A-003): spot_etf ✓, options ✓, futures ✓, short ✓, paper ✓, live ✓ — integrates cleanly

### Policy Logic Verified
- Account lane checks run first (correct priority — reject bad account before checking strategy)
- Strategy requirement mapping is complete (all 8 capability flags covered)
- `enforce_order_intent_capabilities` raises `CapabilityValidationError` for hard rejection — good for wiring into execution paths
- Missing capabilities returned as sorted list — deterministic for testing

### Minor Observations (non-blocking)
- `CapabilityOrderIntent.broker_name` is a string but not currently used in validation — fine for audit trail, just noting for A-004 router design
- Could consider adding `supports_fx` in a future phase for forex pairs, but not needed now

## Decision
Merge-ready. No changes required.
