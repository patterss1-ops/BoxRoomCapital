## Context
Ticket `A-011` creates a single integrated Phase A candidate branch by incorporating the reviewed `A-003` IBKR adapter into the already-completed Codex Phase A stack.

## Changes
- Integrated reviewed A-003 code artifacts into this branch:
  - `broker/ibkr.py`
  - `tests/test_ibkr.py`
- Preserved existing Codex Phase A work already on the base branch:
  - order intent envelope, routing policy, ledger/reconcile API, risk gate, UI surfaces, reliability checks, deprecation cleanup, ledger panel wiring.

## Tests/Checks
- `python3 -m pytest -q tests/test_ibkr.py`
  - `47 passed`
- `python3 -m pytest -q tests`
  - `98 passed, 1 warning` (urllib3/OpenSSL environment warning only)
- `./ops/collab/release-checks/run_phase_a_checks.sh`
  - all checks passed

## Risks
- Regression impact: moderate-low. Change is additive but large (new adapter + 47 tests).
- Rollback note: revert commit `50aa094`.
- Integration note: this branch is the candidate convergence point; final merge order should still respect queue review and conflict resolution for collaboration metadata files.

## Next Action
- Owner: codex/claude shared
- Action: run manual startup smoke (`run_console.py`) and verify Overview/Trading ledger panel + IBKR health surfaces in live app session before merging to `main`.

## Blockers
- none
