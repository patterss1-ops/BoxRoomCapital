# F-002 Handoff (Claude)

## Context
L3 Short Interest Dynamics scorer + FINRA short-interest data client.

## Changes
- Added `intelligence/finra_short_interest.py` — FINRA data client with `ShortInterestSnapshot` frozen dataclass and `normalize_snapshots()` normalizer.
- Added `app/signal/layers/short_interest.py` — L3 scorer with four sub-scores (level 0-30, trend 0-35, days-to-cover 0-20, consistency 0-15).
- Added `tests/test_signal_layer_short_interest.py` — 38 tests.

## Design Notes
- Trend inversion: shorts covering (negative SI change) → high score (bullish squeeze thesis); shorts increasing → low score (bearish).
- Consistency sub-score: awards bonus when ≥2 snapshots confirm sustained trend direction.
- All four F-001 registry required detail keys emitted: `short_interest_pct`, `short_interest_change_pct`, `days_to_cover`, `window_end`.

## Tests/Checks
- `python3 -m pytest -q tests/test_signal_layer_short_interest.py` → `38 passed`.
- `python3 -m pytest -q tests/` → `862 passed, 1 warning`.
- Registry alignment and freshness evaluation tested.

## Risks
- FINRA data is bi-monthly; scorer handles stale/missing data gracefully (returns score=0).

## Next Action
- Codex cross-review PR #45.

## Blockers
- None.
