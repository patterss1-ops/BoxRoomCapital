# F-001 Handoff — Layer Registry + Freshness Contract Freeze

## Context
Phase F split was ACKed by Claude and merged to `main`. `F-001` is the contract-freeze prerequisite for parallel layer tickets `F-002/F-003/F-004/F-005`.

## Changes
- Added new module `app/signal/layer_registry.py`:
  - frozen `LayerContract` metadata for all `L1..L8`
  - canonical freshness thresholds (`warn_age_hours`, `max_age_hours`)
  - frozen required detail-key contracts, including explicit keys for new Phase F layers:
    - `L3_SHORT_INTEREST`
    - `L5_CONGRESSIONAL`
    - `L6_NEWS_SENTIMENT`
    - `L7_TECHNICAL`
  - helper APIs:
    - `get_layer_contract`
    - `list_layer_contracts`
    - `evaluate_freshness`
    - `missing_required_details`
    - `layer_health_snapshot`
    - `validate_layer_registry`
    - `required_detail_keys`
- Updated `app/signal/contracts.py` with non-breaking metadata/freshness hooks on `LayerScore`:
  - `as_of_datetime()`
  - `age_hours(reference_as_of)`
  - `missing_detail_keys(required_keys)`
  - ISO parser refactor to return tz-aware datetimes
- Added deterministic tests in `tests/test_signal_layer_registry.py`.

## Tests/Checks
- `python3 -m pytest -q tests/test_signal_layer_registry.py tests/test_signal_contracts.py` -> `18 passed`
- `python3 -m pytest -q tests/test_signal_composite.py tests/test_signal_shadow_api.py tests/test_signal_engine_e2e.py` -> `56 passed`
- `python3 -m pytest -q` -> `744 passed, 1 warning`

## Risks
- Freshness thresholds are intentionally conservative defaults and may require calibration in `F-006` when stale-layer policy is tuned.
- Required detail-key contracts for new layers are frozen now; downstream tickets should treat missing keys as contract violations or explicit degraded mode.

## Next Action
- Claude cross-reviews `F-001`.
- On approval: merge and unblock `F-002/F-003/F-005` parallel implementation.

## Blockers
- None.
