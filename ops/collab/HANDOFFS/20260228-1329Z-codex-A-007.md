## Context
Ticket `A-007` adds control-plane visibility for Phase A with broker health and order intent audit drill-down surfaces in API + HTMX fragments.

## Changes
- Added API endpoints in `app/api/server.py`:
  - `GET /api/broker-health`
  - `GET /api/order-intents`
  - `GET /api/order-intents/{intent_id}`
- Added fragment endpoints:
  - `GET /fragments/broker-health`
  - `GET /fragments/intent-audit`
- Added intent data helpers with graceful fallback:
  - Uses `data.order_intent_store` when available.
  - Falls back to `order_actions` when `order_intents` module/tables are not present on branch/runtime.
- Added broker health helper:
  - surfaces engine/broker readiness, connection state, kill-switch influence, and capabilities.
- Added UI panels:
  - Overview + Trading now include broker health and intent audit panels.
  - New templates:
    - `app/web/templates/_broker_health.html`
    - `app/web/templates/_intent_audit.html`
- Added API test coverage:
  - `tests/test_api_phase_a_surfaces.py`
  - endpoint behavior, fallback behavior, fragment rendering, and 404 detail handling.

## Tests/Checks
- `python3 -m pytest -q tests/test_api_phase_a_surfaces.py`
  - `4 passed`
- `python3 -m pytest -q tests`
  - `40 passed, 5 warnings`
- `./ops/collab/release-checks/run_phase_a_checks.sh`
  - full suite and focused checks both pass

## Risks
- Regression impact: moderate-low; API/template additions are additive and do not alter existing action handlers.
- Rollback note: revert commit `f0417f6` to remove new surfaces if operational issues appear.
- Dependency note: unified multi-broker ledger/NAV panel remains dependent on `A-005`; this ticket surfaces broker health + intent audit now and uses fallback data when intent store is unavailable.

## Next Action
- Owner: codex/claude shared
- Action: once `A-005` lands, add ledger attribution panel and wire it into the same readiness view; then remove fallback-only assumptions where `order_intents` is guaranteed.

## Blockers
- none
