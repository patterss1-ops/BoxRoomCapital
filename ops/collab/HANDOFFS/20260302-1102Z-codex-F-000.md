# F-000 Handoff — Phase F Bootstrap + Split Proposal

## Context
User requested Phase F planning in collaboration with Claude immediately after Phase E completion.

## Changes
- Added `DEC-022` in `ops/collab/DECISIONS.md` to define lane name and objective:
  - `Phase F: Tier-1 Completion + Intelligence Ingest`
- Added queue rows `F-000` through `F-008` in `ops/collab/TASK_QUEUE.md`.
- Added ownership locks for `F-000` through `F-008` in `ops/collab/OWNERSHIP_MAP.md`.
- Marked `F-000` as `IN_PROGRESS` (Codex) for planning/coordination.
- Posted ACK-required mailbox split proposal:
  - `ops/collab/mailbox/inbox/20260302-1102Z-codex-to-claude-phasef-split-proposal.md`

## Proposed build sequence
1. `F-001` contract freeze.
2. Parallel layer build:
   - `F-002` (L3), `F-003` (L5), `F-004` (L6), `F-005` (L7).
3. `F-006` composite v3 + stale-layer policy.
4. `F-007` shadow jobs + operator ranking surface.
5. `F-008` acceptance harness + release checks.

## Tests/Checks
- Metadata-only update (collab planning artifacts); no runtime code changes.
- Note: `collab_guard --agent claude` is expected to block until Phase F split ACK is received.

## Risks
- Ticket ownership/dependencies may need edits based on Claude feedback; split is not final until ACK.

## Next Action
- Await Claude ACK (or requested edits) on Phase F split proposal.
- On ACK: update `F-000` to `DONE` and release `F-000` lock; execution can begin at `F-001`.

## Blockers
- Cross-agent ACK pending for `20260302-1102Z-codex-to-claude-phasef-split-proposal.md`.
