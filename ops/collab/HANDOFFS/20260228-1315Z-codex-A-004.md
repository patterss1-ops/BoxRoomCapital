## Context
Ticket `A-004` implements deterministic account routing with explicit machine-readable reject reasons, enforcing policy controls before broker submission.

## Changes
- Added `execution/router.py`:
  - `RouteIntent`, `RouteConfigEntry`, `RouteOverride`
  - `AccountRouter.resolve(...)` with deterministic route resolution order:
    1. `strategy:<strategy_id>`
    2. `sleeve:<sleeve>`
    3. `default`
  - Pre-trade policy gates for:
    - kill switch
    - market cooldown
    - missing broker registration
    - capability incompatibility
- Added `execution/policy/route_policy.py`:
  - `RouteRejectCode`
  - `RoutePolicyState`
  - `RouteResolution`
  - `RouteDecision`
- Updated `execution/policy/__init__.py` exports for route policy primitives.
- Added `tests/test_account_router.py`:
  - deterministic routing behavior
  - explicit reject codes for invalid route scenarios
  - same policy path for manual (`operator`) and automated (`system`) actions
  - override path still validated by capability checks

## Tests/Checks
- `python3 -m pytest -q tests/test_account_router.py`
  - `4 passed`
- `python3 -m pytest -q tests`
  - `26 passed, 3 warnings` (existing environment/framework warnings only)

## Risks
- Current router uses in-memory route map inputs (no runtime config loader yet); integration wiring into control-plane execution paths is still pending.
- `A-003` (IBKR adapter) is still in progress, so real broker registration for IBKR routes depends on that ticket landing.

## Next Action
- Wire `AccountRouter` into execution entry points so all automated/manual submissions use the same router call.
- Add config-backed route map bootstrap once A-003 broker keys are finalized.

## Blockers
- None.
