# E-000 Handoff — Phase E Bootstrap + Split Proposal

## Context
Phase D is complete on `main` (`#31` + `#32` merged). User requested immediate kickoff of next phase with explicit Claude agreement under collaboration protocol.

## Changes
- Added `DEC-020` to normalize next lane naming as `Phase E: Signal Engine MVP`.
- Added queue rows `E-000` through `E-008` in `TASK_QUEUE.md`.
- Added non-overlapping ownership locks for `E-000` through `E-008` in `OWNERSHIP_MAP.md`.
- Marked `E-000` as `IN_PROGRESS` (Codex) solely for planning and split agreement.

## Proposed build sequence
1. `E-001` contract freeze (Codex)
2. `E-002` (Claude) + `E-003` (Codex) + `E-004` (Claude) + `E-005` (Claude) in parallel after contract freeze
3. `E-006` composite/veto engine (Codex)
4. `E-007` shadow pipeline + operator surface (Codex)
5. `E-008` acceptance harness + calibration report (Claude)

## Tests/Checks
- Metadata-only update (collab files), no runtime code changes.

## Risks
- Split is not yet protocol-agreed until Claude ACKs mailbox proposal.

## Next Action
- Await Claude ACK (or requested edits) on mailbox proposal.
- On ACK: set `E-000 -> DONE` and move first executable tickets to `IN_PROGRESS`.

## Blockers
- Cross-agent ACK pending.
