## Context
Ticket `A-008` adds regression and reliability safeguards so existing IG operational paths stay stable while multi-broker features are layered in.

## Changes
- Added startup-recovery reliability tests in `tests/test_startup_recovery_reliability.py`:
  - recovery success path (pending open action resolved to completed)
  - stale position sync failure path
  - broker timeout failure-injection path
- Added incident/audit failure tests in `tests/test_failure_incident_audit.py`:
  - failed order action appears in incidents with correlation ID and error detail
  - retrying order action appears with `RETRYING` category and preserved audit fields
- Added release checks assets:
  - `ops/collab/release-checks/PHASE_A_RELEASE_CHECKLIST.md`
  - `ops/collab/release-checks/run_phase_a_checks.sh`
- Release runner supports stacked branches by executing focused suites only for files present in the current branch.

## Tests/Checks
- `python3 -m pytest -q tests/test_startup_recovery_reliability.py tests/test_failure_incident_audit.py`
  - `5 passed`
- `python3 -m pytest -q tests`
  - `36 passed, 3 warnings`
- `./ops/collab/release-checks/run_phase_a_checks.sh`
  - full suite pass + focused reliability suite pass

## Risks
- Regression impact: low; changes are additive tests + release check assets only.
- Rollback note: revert commit `6c5ad4a` if needed; no runtime path modifications were introduced.

## Next Action
- Owner: codex/claude shared
- Action: integrate `A-002` intent envelope and `A-006` risk decision payloads into execution entry points, then extend release checklist to verify those UI/API drill-downs post `A-007`.

## Blockers
- none
