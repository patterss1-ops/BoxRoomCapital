# A-007 Handoff

## Context
- Ticket: A-007 — Control Plane Phase A Surfaces
- Branch: `claude/a-007-control-plane-phase-a`
- Previous status: `BACKLOG`
- Target status: `REVIEW`

## Changes

### New DB functions in `data/trade_db.py`
- `get_risk_verdicts(limit, approved, ticker, db_path)` — query risk verdicts with filters, JSON-parse details
- `get_risk_verdict_summary(db_path)` — aggregate stats: total/approved/rejected/rate + top rejection rules

### New JSON API endpoints in `app/api/server.py` (~100 lines)

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/broker/accounts` | GET | List registered broker accounts (filter: broker, active_only) |
| `/api/broker/positions` | GET | Unified positions across all brokers (filter: broker, sleeve) |
| `/api/broker/cash` | GET | Latest cash balances per broker account |
| `/api/broker/health` | GET | Broker connection health status (aggregated by broker) |
| `/api/nav/history` | GET | NAV history (filter: level, level_id, days) |
| `/api/risk/verdicts` | GET | Pre-trade risk gate verdicts (filter: approved, ticker) |
| `/api/risk/summary` | GET | Risk verdict summary statistics |
| `/api/reconciliation/reports` | GET | Multi-broker reconciliation reports (filter: broker_account_id) |

### New HTMX fragment endpoints (~60 lines)

| Fragment | Auto-refresh | Description |
|----------|-------------|-------------|
| `/fragments/broker-health` | 15s | Broker health panel with account details |
| `/fragments/ledger-positions` | 10s | Unified positions table (filterable by broker/sleeve) |
| `/fragments/ledger-cash` | 15s | Cash balances with total |
| `/fragments/risk-verdicts` | 10s | Risk verdict table + summary stats |
| `/fragments/nav-history` | 30s | NAV history table |
| `/fragments/reconciliation-reports` | 30s | Reconciliation report history |

### New HTML page
- `/fund` page — Fund dashboard with all Phase A panels loaded via HTMX

### New templates (7 files)
- `fund_page.html` — Main fund page with hero grid + HTMX panels
- `_broker_health.html` — Broker health table
- `_ledger_positions.html` — Unified positions table (11 columns)
- `_ledger_cash.html` — Cash balances table with total
- `_risk_verdicts.html` — Risk verdicts with summary stats + table
- `_nav_history.html` — NAV history table
- `_reconciliation_reports.html` — Reconciliation reports with mismatch details

### Navigation
- Added "Fund" link to `base.html` nav bar (between Trading and Research)

### Tests — `tests/test_control_plane_a007.py` (~350 lines, 46 tests)
- TestGetRiskVerdicts: 7 tests (DB function)
- TestGetRiskVerdictSummary: 4 tests (DB function)
- TestBrokerAccountsAPI: 2 tests (JSON API)
- TestBrokerPositionsAPI: 3 tests (JSON API)
- TestBrokerCashAPI: 2 tests (JSON API)
- TestBrokerHealthAPI: 2 tests (JSON API)
- TestNavHistoryAPI: 2 tests (JSON API)
- TestRiskVerdictsAPI: 5 tests (JSON API)
- TestReconciliationAPI: 2 tests (JSON API)
- TestFundPage: 2 tests (HTML page)
- TestNavigation: 1 test (nav bar)
- TestHTMXFragments: 8 tests (all 6 fragments + filter + empty state)
- TestExistingEndpoints: 6 tests (regression guard)

## Tests/Checks
- `python3 -m pytest tests/test_control_plane_a007.py -v` → 46 passed
- `python3 -m pytest tests/ -v` → 203 passed (full suite, zero regressions)
- No existing IG options flow impacted

## Architecture Notes
- All Phase A data surfaces read from A-005 ledger and A-006 risk verdicts
- HTMX auto-refresh intervals: health/cash 15s, positions/verdicts 10s, NAV/recon 30s
- Empty state handling: all fragments show placeholder text when no data present
- API follows existing pattern: `{"items": [...]}` for lists, direct dict for summaries
- Test fixture uses `mock.patch.object` on `get_conn` to route all DB calls to test DB, avoiding default parameter binding issues

## Risks
- **Regression impact**: Low. All new routes/templates are additive. Only change to existing file is nav bar addition in base.html.
- **Rollback note**: Delete new templates, remove API routes from server.py, revert base.html nav, delete test file, revert trade_db.py additions.
- **Known limitation**: Broker health check is registration-based only — actual live connection health checks (calling `broker.health_check()`) require A-004 router integration.
- **Known limitation**: Fund page hero-grid KPI values are static placeholders — will wire to JS once data flows are live.

## Next Action
- Owner: codex (cross-review)
- Action: Cross-review A-007. Wire KPI cards with JS to pull from API endpoints.

## Blockers
- None for the surfaces themselves. Data will populate as brokers connect and trades flow through the risk gate.
