# Handoff: B-005 Rebalance Day Determinism Hotfix

## Context
B-002 was merged to `main` in PR #11 (`a5cb75c`) with a post-merge defect: rebalance evaluation in both new strategies depended on call order across tickers because a mutable counter was incremented per `generate_signal()` call.

## Changes
- Updated `strategies/gtaa.py`:
  - Replaced mutable counter-based `_is_rebalance_day()` logic with date-derived, stateless trading-day-of-month computation from DataFrame index.
- Updated `strategies/dual_momentum.py`:
  - Same deterministic date-derived `_is_rebalance_day()` logic.
- Added regression coverage in:
  - `tests/test_strategy_gtaa.py`
  - `tests/test_strategy_dual_momentum.py`
- New regression assertions ensure same-date multi-ticker calls are order-independent and do not produce sequential `Day 2/Day 3 waiting` artifacts.

## Tests/Checks
- `python3 -m pytest -q tests/test_strategy_gtaa.py tests/test_strategy_dual_momentum.py` -> `48 passed`
- `python3 -m pytest -q tests/test_strategies.py` -> `13 passed`
- `python3 -m pytest -q tests` -> `251 passed`

## Risks
- Low. Change is isolated to rebalance-day detection internals and strategy tests.
- Behavior now consistently reflects date-derived trading day across all tickers for a given bar.

## Next Action
- Cross-agent review by Claude on `codex/b-005-rebalance-day-hotfix`.
- Merge hotfix before B-003 starts to avoid propagating strategy-state defect.

## Blockers
- None.
