# Handoff: B-001 TradingView Webhook Intake

## Context
Phase B Wave 1 split was ACKed by Claude. Ticket B-001 is Codex-owned with exclusive scope for TradingView webhook intake in API/config/tests.

## Changes
- Added webhook helper module: `intelligence/webhook_server.py`.
- Added config controls in `config.py`:
  - `TRADINGVIEW_WEBHOOK_TOKEN`
  - `TRADINGVIEW_WEBHOOK_MAX_PAYLOAD_BYTES`
- Added authenticated endpoint in `app/api/server.py`:
  - `POST /api/webhooks/tradingview`
  - Validates JSON payload size/shape and token (header/query/payload token fallback).
  - Logs explicit audit events:
    - accept -> `SIGNAL`
    - reject -> `REJECTION`
  - Returns structured errors (`invalid_payload`, `missing_token`, `invalid_token`, `webhook_not_configured`, `payload_too_large`).
- Added endpoint tests in `tests/test_api_webhook_intake.py`:
  - valid payload accepted
  - invalid token rejected + audited
  - invalid payload rejected + audited
  - missing config token rejected + audited

## Tests/Checks
- `python3 -m pytest -q tests/test_api_webhook_intake.py` -> `4 passed`
- `python3 -m pytest -q tests/test_api_phase_a_surfaces.py` -> `6 passed`
- `python3 -m pytest -q tests/test_api_research_jobs.py` -> `2 passed`
- `python3 -m pytest -q tests` -> `203 passed`

## Risks
- Endpoint currently audits only into `bot_events`; no separate webhook inbox table yet.
- No replay protection yet (timestamp/signature nonce). This should be considered if webhook throughput grows.

## Next Action
- Cross-agent review by Claude on `codex/b-001-tradingview-webhook-intake`.
- If accepted, merge B-001 and then proceed to B-004 after B-003 dependency is complete.

## Blockers
- None.
