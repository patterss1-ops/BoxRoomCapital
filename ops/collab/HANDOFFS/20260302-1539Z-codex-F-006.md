# F-006 Handoff — Composite v3 calibration + stale-layer policy

## Context
`F-002/F-003/F-004/F-005` are merged on `main`. `F-006` calibrates composite scoring with explicit data-quality behavior and freshness-aware shadow-cycle handling.

## Changes
- Updated `app/signal/composite.py`:
  - Added required-layer and freshness-aware data-quality configuration on `CompositeScoringConfig`.
  - Added deterministic quality evaluation (`missing_required`, `warning_layers`, `stale_layers`).
  - Added quality penalty application after convergence multiplier.
  - Added optional veto emission controls for missing/stale data and surfaced diagnostics in `CompositeResult.notes`.
- Updated `app/engine/signal_shadow.py`:
  - Added freshness annotations (`_freshness_state`, `_age_hours`, `_observed_as_of`) when cloning layer payloads.
  - Added configurable strict mode (`enforce_required_layers`) for hard-blocking missing required layers.
  - Enforced stale-layer blocking in shadow via hard-block veto policy.
  - Added explicit summary counters for blocked/stale and scored-with-missing-required diagnostics.
- Added/updated tests:
  - `tests/test_signal_composite.py` for required-layer veto and freshness penalty regression coverage.
  - `tests/test_signal_shadow_api.py` for missing-required and stale-layer blocking behavior.
- Posted Claude sync message:
  - `ops/collab/mailbox/inbox/20260302-1528Z-codex-to-claude-f006-claim-sync.md`

## Tests/Checks
- `python3 -m pytest -q tests/test_signal_engine_e2e.py tests/test_signal_composite.py tests/test_signal_shadow_api.py` -> 60 passed
- `python3 -m pytest -q` -> 892 passed
- `python3 ops/collab/tools/collab_guard.py --agent codex` -> pass

## Risks
- Missing required layers are soft-scored by default (`enforce_required_layers=False`) for backward compatibility; strict full-tier enforcement now requires enabling `enforce_required_layers=True` at call sites.
- Existing `claude` mailbox guard has an unrelated historical OPEN/ACK mismatch (`20260302-1102Z-codex-to-claude-phasef-split-proposal.md`).

## Next Action
Claude cross-review on F-006. If approved, merge F-006 and proceed to `F-007` (tier-1 shadow jobs + ranking operator surface).

## Blockers
None for F-006 implementation itself.
