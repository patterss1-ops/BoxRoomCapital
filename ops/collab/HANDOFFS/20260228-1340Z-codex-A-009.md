## Context
Ticket `A-009` cleans up active FastAPI/Starlette deprecations observed in the test suite so future work is not masked by framework warning noise.

## Changes
- Updated app startup lifecycle in `app/api/server.py`:
  - Replaced deprecated `@app.on_event("startup")` hook with a FastAPI `lifespan` context manager.
  - Initialization still runs `init_db()` at app startup.
- Updated template rendering calls in `app/api/server.py`:
  - Converted all `Jinja2Templates.TemplateResponse` calls to request-first signature:
    - from `TemplateResponse("name.html", context)`
    - to `TemplateResponse(request, "name.html", context)`
  - This removes Starlette template signature deprecation warnings.

## Tests/Checks
- `python3 -m pytest -q tests/test_api_phase_a_surfaces.py tests/test_api_ledger.py`
  - `6 passed, 1 warning` (urllib3/LibreSSL environment warning only)
- `python3 -m pytest -q tests`
  - `49 passed, 1 warning` (urllib3/LibreSSL environment warning only)
- `./ops/collab/release-checks/run_phase_a_checks.sh`
  - all checks passed

## Risks
- Regression impact: low. Changes are framework-signature migrations only and preserve response payload/context.
- Rollback note: revert commit `7e69434`.
- Remaining warning: urllib3/OpenSSL warning is environment-level and unrelated to application logic.

## Next Action
- Owner: codex/claude shared
- Action: when Phase A branches are merged, run one full clean-room test pass in a fresh environment to confirm no framework deprecation warnings regress.

## Blockers
- none
