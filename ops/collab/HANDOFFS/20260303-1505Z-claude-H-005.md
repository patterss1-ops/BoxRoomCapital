---
ticket: H-005
title: End-of-day reconciliation automation + P&L attribution
owner: claude (took over from codex for speed)
status: DONE
branch: claude/h-001-promotion-enforcement
date: 2026-03-03T15:05:00Z
---

## Deliverables

### fund/eod_reconciliation.py
- `EODReconciliationReport` dataclass — full reconciliation report with position mismatches, strategy P&L, sleeve P&L
- `run_eod_reconciliation()` — main batch job:
  1. Reconciles positions across all active broker accounts
  2. Attributes realised P&L by strategy (closed trades)
  3. Attributes unrealised P&L by sleeve (broker positions)
  4. Persists report to `reconciliation_reports` table
- `dispatch_eod_reconciliation()` — scheduler callback for post-market-close hook
- Supporting dataclasses: `PositionMismatch`, `StrategyPnL`, `SleevePnL`

### tests/test_eod_reconciliation.py — 14 tests
- Section 1: Data class serialization (4 tests)
- Section 2: Core reconciliation logic (7 tests)
  - Clean DB, strategy P&L attribution, sleeve P&L attribution
  - Phantom position detection, report persistence
  - Multi-account reconciliation, default date
- Section 3: Scheduler dispatch integration (3 tests)

## Integration points
- Reads from: `broker_accounts`, `broker_positions`, `trades` tables
- Writes to: `reconciliation_reports` table
- Scheduler: `dispatch_eod_reconciliation(window_name, db_path)` for `DailyWorkflowScheduler`

## Test evidence
```
14 passed in 3.19s
```

## Bug fix included
- Fixed `app/metrics.py`: `created_at` → `timestamp` column name in `bot_events` query (schema mismatch from H-003)
