# A-003 Handoff

## Context
- Ticket: A-003 — IBKR Paper Adapter (MVP)
- Branch: `claude/a-003-ibkr-paper-adapter`
- Previous status: `IN_PROGRESS`
- Target status: `REVIEW`

## Changes
- Created `broker/ibkr.py` (~370 lines):
  - `IBKRBroker` class implementing `BaseBroker` interface via `ib_async`
  - Capability declaration: spot_etf, options, futures, short, paper, live (no spreadbet/cfd)
  - connect/disconnect with TWS/IB Gateway (paper port 7497, live port 7496)
  - get_account_info: parses NetLiquidation, TotalCashValue, UnrealizedPnL
  - get_positions/get_position: maps IBKR positions to BaseBroker Position model
  - place_long/place_short: market orders via `_place_order` internal method
  - place_limit_order: limit orders with explicit price
  - close_position: reverses direction based on current position
  - cancel_order: cancels by tracked order_id
  - get_order_status: returns status dict from tracked Trade objects
  - get_open_orders: lists all active unfilled orders
  - health_check: connection state + server time for control plane monitoring
  - Lazy ib_async import with fallback stand-in classes for test environments
  - Dependency injection via `ib_client` parameter for testability

- Created `tests/test_ibkr.py` (~470 lines, 47 tests):
  - TestCapabilities: 9 tests — capability matrix correctness
  - TestConnection: 6 tests — connect, disconnect, failure handling
  - TestAccountInfo: 2 tests — account parsing, disconnected state
  - TestPositions: 7 tests — long/short/empty/not-connected/lookup
  - TestOrderPlacement: 8 tests — market, limit, qualify failure, broker error, close
  - TestOrderManagement: 6 tests — cancel, status lookup, open orders
  - TestHealthCheck: 3 tests — connected, disconnected, error
  - TestIntegrationFlow: 3 tests — full lifecycle, cancel lifecycle, connect-place-disconnect
  - TestCapabilityPolicyIntegration: 4 tests — validates A-001 policy works with IBKR adapter

- Updated `config.py`: added IBKR_HOST, IBKR_PORT, IBKR_CLIENT_ID, IBKR_ACCOUNT, IBKR_CURRENCY
- Updated `.env.example`: added IBKR environment variable documentation
- Updated `requirements.txt`: added `ib_async>=1.0.0`

## Tests/Checks
- `python3 -m pytest tests/test_ibkr.py -v` → 47 passed
- `python3 -m pytest tests/ -v` → 69 passed (full suite, zero regressions)
- No existing IG options flow impacted

## Risks
- **Regression impact**: Low. No existing code modified except config additions. The IBKR adapter is a new file with no wiring into existing execution paths yet.
- **Rollback note**: Delete `broker/ibkr.py`, `tests/test_ibkr.py`, and revert config/requirements changes.
- **Known limitation**: `entry_time` on positions uses `datetime.now()` since IBKR doesn't provide entry time via `positions()` API. Will be resolved when ledger (A-005) tracks fills.
- **Known limitation**: Strategy attribution on positions is empty (`""`) — will be resolved by A-005 ledger mapping.

## Next Action
- Owner: codex (cross-review)
- Action: Cross-review A-003 implementation. If approved, merge and unblock A-004 and A-005.

## Blockers
- None. A-001 dependency satisfied (capability contract integrated and tested).
